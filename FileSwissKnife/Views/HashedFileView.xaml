<UserControl x:Class="FileSwissKnife.Views.HashedFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:customControls="clr-namespace:FileSwissKnife.CustomControls"
             xmlns:viewModels="clr-namespace:FileSwissKnife.ViewModels"
             mc:Ignorable="d" 
             d:DesignWidth="400" d:DesignHeight="100" d:DataContext="{d:DesignInstance viewModels:HashedFileViewModel}">

    <Border BorderThickness="1" BorderBrush="{StaticResource FrameBorderBrush}">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" IsReadOnly="True" MinHeight="40" Text="{Binding Path=TextResult, Mode=OneWay}"
                     FontFamily="Courier New" />

            <Grid Grid.Row="1" Grid.Column="0">
                <customControls:ProgressBarExt Height="18"
                                               Visibility="{Binding Path=ProgressBarVisibility}"
                                               Text="{Binding Path=ProgressBarText}"
                                               Value="{Binding Path=ProgressBarValue}" Margin="0,3,0,0" />
            </Grid>


            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="5,0,0,0">
                    <Button 
                        Margin="0" Width="20" Height="20" Style="{StaticResource CloseButton}"
                        Command="{Binding Path=CloseCommand}" />
                    <customControls:PlayStopButton 
                        Margin="0,5,0,0" Width="20" Height="20" PlayStopButtonState="{Binding State}" 
                        Command="{Binding Path=HashOrCancelCommand}"
                        />
                </StackPanel>


            </Grid>

        </Grid></Border>
</UserControl>
