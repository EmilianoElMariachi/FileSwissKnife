<Window x:Class="FileSwissKnife.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileSwissKnife"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" AllowDrop="True" Drop="MainWindow_OnDrop">
    <Window.Resources>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <TabControl x:Name="TabControl">
            <TabItem x:Name="TabItemJoin"  Header="Join">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Input files:" />
                        <TextBox x:Name="FilesToJoin" Grid.Row="1" AllowDrop="True" PreviewDragOver="FilesToJoin_OnPreviewDragOver" />
                    </Grid>

                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />

                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Output file:" />
                        <TextBox x:Name="JoinOutputFilePath" Grid.Row="1" />
                    </Grid>

                </Grid>
            </TabItem>
            <TabItem x:Name="TabItemSplit" Header="Split">

            </TabItem>
            <TabItem x:Name="TabItemHash" Header="Hash">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Input files:" />
                        <TextBox x:Name="FilesToHash" Grid.Row="1" AllowDrop="True" PreviewDragOver="FilesToJoin_OnPreviewDragOver" />
                    </Grid>

                </Grid>
            </TabItem>
        </TabControl>
        <StackPanel Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"  />
                    <ColumnDefinition Width="5"  />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Click="StartCancelButton_Click" HorizontalAlignment="Center" Grid.Column="0" MinWidth="50">Go</Button>
                <Grid Grid.Column="2">
                    <ProgressBar x:Name="ProgressBar"  Minimum="0" Maximum="100.0" Height="20" />
                    <Label x:Name="ProgressLabel" HorizontalAlignment="Center">100</Label>
                </Grid>

            </Grid>

        </StackPanel>
    </Grid>
</Window>
